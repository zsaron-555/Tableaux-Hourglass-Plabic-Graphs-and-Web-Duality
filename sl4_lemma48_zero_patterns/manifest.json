{
  "schema": "sl4_lemma48_zero_manifest_v1",
  "description": "GL4 specialization of the generalized BCGMMW Lemma 4.8 zero-discharge rule. The local interval has variable length, so the JSON records detector parameters and the theorem-shaped local configuration.",
  "patterns": [
    {
      "id": "sl4_lemma48_gl4_interval_fan_chain",
      "file": "gl4_interval_fan_chain.json"
    }
  ]
}
